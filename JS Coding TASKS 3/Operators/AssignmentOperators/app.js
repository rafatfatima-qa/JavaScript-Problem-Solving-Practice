/*

2. Problem Name: AssignmentOperators:

What will happen if:

x=3; x+1; x+=3; 

*/
        let x=3;
        x+1; 
        x+=3;
        console.log(x);

        //Expected output: 6 because value of x+1 has only computed but not assigned back to x.


